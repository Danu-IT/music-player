import React from "react";
import { ContainerPage, Page } from "../layouts/components";

type Props = {};

const Playlists = (props: Props) => {
  return (
    <Page>
      <ContainerPage>Playlists</ContainerPage>
    </Page>
  );
};

export default Playlists;
